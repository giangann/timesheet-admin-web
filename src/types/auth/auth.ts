export type TCredentials = {
  identifyCard: string;
  password: string;
};

export type TUserInfo = {
  name: string;
  email: string;
  address: string;
  phone: string;
  identifyCard: string;
  roleName: string;
  roleCode: string;
};
